---
// API keys are now one tab of the unified Access page (/app/settings/access).
// Redirect so old links / bookmarks still resolve.
const auth = Astro.locals.auth;
return Astro.redirect(auth ? '/app/settings/access' : '/auth/login');
---
